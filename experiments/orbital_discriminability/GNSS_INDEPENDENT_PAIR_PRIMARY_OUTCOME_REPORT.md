# ALGO/MDO DOY219 primary outcome

The single authorized execution stopped with:

`PRIMARY_ARTIFACT_MATERIALIZATION_FAILED`

The first frozen locator, ALGO00CAN, returned a transport timeout
(`WinError 10060`) before a complete artifact could be materialized. The
artifact list is empty, the MDO100USA locator was not attempted, and the
held-out comparison is `NOT_EVALUATED`.

This is a descriptive execution failure, not `MEASUREMENT_INVALID` and not a
physical result. It authorizes no orbital or null-model claim. The frozen
zero-retry rule forbids another attempt, endpoint substitution, date change,
fallback or reserve.

The strict-JSON outcome receipt is 568 bytes with SHA-256:

`c510db1488c8cbb19b16d5b34150fec2334efddb4a2cfaa3e94a03cba0963ab3`

No complete observation artifact, header or observation value was admitted or
persisted. The terminal scope is the exact ALGO/MDO DOY219 experiment only.
