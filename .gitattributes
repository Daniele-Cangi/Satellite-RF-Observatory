# Receipt artifacts are byte-addressed evidence; never normalize line endings.
experiments/live_instrument/session_receipts/*.jsonl -text

# Machine-readable experiment evidence is frequently bound by byte count and
# SHA-256. Preserve repository bytes on every platform instead of adding one
# filename exception after each Windows checkout failure.
experiments/**/*.json -text
experiments/**/*.jsonl -text

# This historical parser receipt binds the repository's LF source blob. Force
# the same checkout bytes on Windows; the regression also canonicalizes CRLF
# so a pre-existing checkout cannot produce a false provenance failure.
experiments/orbital_discriminability/doris_development_header.py text eol=lf

# Pinned protocol evidence is also verified by its byte-exact SHA-256.
experiments/live_instrument/protocol_sources/gate_f2_5_17/rx_sound_cmd.h -text

# These frozen development artifacts are joined by byte-exact manifest hashes.
experiments/orbital_discriminability/maven_dss45_detector_develop.py -text
experiments/orbital_discriminability/MAVEN_DSS45_CARRIER_DETECTOR_MANIFEST.json -text
experiments/orbital_discriminability/MAVEN_DSS45_DETECTOR_DEVELOPMENT_RESULT.json -text

# This frozen Cassini parent receipt was sealed from its exact CRLF byte stream.
experiments/orbital_discriminability/CASSINI_DUAL_ROOT_XKA_COMPILER_RECEIPT.json -text whitespace=cr-at-eol

# The terminal GNSS replication receipt is hashed from its exact LF byte stream.
experiments/orbital_discriminability/GNSS_PHASE_REPEATED_PASS_OUTCOME.json -text

# The independent-pair geometry receipt is also byte-addressed evidence.
experiments/orbital_discriminability/GNSS_PHASE_INDEPENDENT_PAIR_SCREEN_RECEIPT.json -text

# The one-shot DOY223 outcome is frozen from its exact CRLF byte stream.
experiments/orbital_discriminability/GNSS_INDEPENDENT_PAIR_DOY223_OUTCOME.json -text whitespace=cr-at-eol

# The synthetic observer-transfer receipt is frozen from its exact LF byte stream.
experiments/orbital_discriminability/GNSS_OBSERVER_TRANSFER_SPIKE_RECEIPT.json -text

# The bounded real-geometry observer-transfer receipt is exact LF evidence.
experiments/orbital_discriminability/GNSS_OBSERVER_TRANSFER_GEOMETRY_RECEIPT.json -text

# The LuGRE metadata audit receipt is frozen from its exact LF byte stream.
experiments/orbital_discriminability/LUGRE_PROSPECTIVE_METADATA_AUDIT_RECEIPT.json -text

# Independent-position evidence retains its frozen bytes across platforms.
experiments/gnss_inverse_positioning/*.py -text
experiments/gnss_inverse_positioning/event_2026249/*.json -text whitespace=cr-at-eol
experiments/positioning_g12_doy250/*.json -text whitespace=cr-at-eol

# Fixed-width SP3 source excerpts include meaningful record padding.
positioning/tests/fixtures/*.sp3 -text whitespace=-blank-at-eol

experiments/positioning_g12_doy248/*.json -text whitespace=cr-at-eol
experiments/positioning_g12_doy248/sources/** -text whitespace=cr-at-eol

# Downloaded dossiers and their catalog hashes must survive Windows checkouts.
web/app/results.json -text
web/public/evidence/*.json -text

# Configured-request evidence is sealed byte-for-byte across platforms.
experiments/positioning_g13_doy247_request/** -text whitespace=cr-at-eol

# Frozen candidate-network evidence.
experiments/positioning_g14_doy246_network/** -text whitespace=cr-at-eol
experiments/positioning_g14_doy246_network/estimation/reference_only.rnx -text whitespace=-blank-at-eol,cr-at-eol

# Preserve the source/report bytes recorded by the synthetic development study.
research/kinematic/*.py -text
research/kinematic/results/*.json -text

# Synthetic fixed-width RINEX fixtures retain padding and exact receipt bytes.
research/kinematic/tests/fixtures/*.rnx -text whitespace=-blank-at-eol
research/kinematic/slow_validation_plan.json -text
# Kinematic source manifests bind these exact historical bytes (including
# mixed LF/CRLF in solver.py). Never normalize them during checkout or add.
positioning/calibration.py -text whitespace=cr-at-eol
positioning/context.py -text whitespace=cr-at-eol
positioning/errors.py -text whitespace=cr-at-eol
positioning/navigation.py -text whitespace=cr-at-eol
positioning/solver.py -text whitespace=cr-at-eol
docs/SCIENTIFIC_ROADMAP.md -text
